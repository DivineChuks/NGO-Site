const childSection = document.getElementById('child-section');

window.addEventListener('scroll', slideChild)

slideChild()

function slideChild(){
    console.log(window.innerHeight)
    const triggerPoint = window.innerHeight / 5 * 4
    
}